-- =====================================================================
-- 0033_terms_agreement_tracking.sql
-- تتبّع موافقة العميل على الشروط والأحكام بربطها بنسخة السياسة (policy)
-- المعروضة وقتها — لو نص الشروط اتغيّر بعدين (نسخة جديدة)، هيتطلب موافقة
-- تانية تلقائيًا، لكن نفس النسخة مش هتتطلب موافقة متكررة في كل طلب
-- =====================================================================

alter table public.users
  add column if not exists terms_agreed_policy_id uuid references public.policies(id);
