-- =====================================================================
-- 0032_support_contact_setting.sql
-- رقم واتساب الدعم/الشكاوى — يتغيّر من لوحة التحكم (سوبر أدمن) ويُستخدم
-- في كل مكان في الواجهة فيه "تواصل معانا" (تسجيل الدخول، الحساب الموقوف،
-- الشكاوى، رفض إثبات الدفع...)
-- =====================================================================

insert into public.platform_settings (key, value) values
  ('support_whatsapp_number', '""'::jsonb)
on conflict (key) do nothing;
