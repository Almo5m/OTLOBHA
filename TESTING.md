# خطة الفحص المحلي

فحصين منفصلين: **أوتوماتيكي** (بشغّله أنا وبتشغّله أنت بأمر واحد) و**يدوي** (لازم إنسان يجربه في المتصفح، مفيش بديل له).

---

## 1. الفحص الأوتوماتيكي (`npm test`)

عندك دلوقتي 40 اختبار (كانوا 33، ضفت منهم اتنين جداد لهذا التحديث: `cart-store.test.ts` و`cloudinary-sign.test.ts` بقى فيه فحص لـ"product-request"). بيغطوا منطق السلة، والتحقق من روابط Cloudinary، والفلترة والبحث، وتعقيم ملفات Excel، وحراسة المسارات.

```
npm install
npm test
```

لازم تشوف `# pass 40` و`# fail 0`. لو ظهر `fail`، ابعتلي النص كامل.

هوعمل بناء إنتاجي كامل (`npm run build`) ونفّذت الاختبارات دي بنفسي على نسخة من المشروع، وعدّت كلها من غير أخطاء. النسخة اللي عندك مطابقة لما فحصته.

---

## 2. الفحص على قاعدة البيانات — قبل أي حاجة تانية

المشروع ده فيه 4 migrations جدد (`0047` لحد `0050`) مش مطبّقين على مشروعك لسه. **متجربش الموقع قبل ما تطبّقهم**، وإلا حاجات زي "طلب منتج مش موجود" هتفشل بغرابة.

1. خد نسخة احتياطية الأول (زي ما اتفقنا قبل كده).
2. طبّق الأربعة بالترتيب من SQL Editor: `0047_hero_image_setting.sql` ← `0048_products_delete_policy.sql` ← `0049_manual_product_requests.sql` ← `0050_free_form_ordering.sql`.
3. تأكد إنهم اتطبقوا:
   ```sql
   select count(*) as should_be_2 from pg_proc
   where proname in ('mark_product_request_converted', 'dismiss_product_request');
   ```
   ```sql
   select column_name from information_schema.columns
   where table_name = 'order_items' and column_name in ('target_price', 'manual_category_id', 'manual_image_url', 'converted_product_id', 'dismissed_at');
   -- لازم يرجع 5 أعمدة
   ```

اختبرت أنا الأربعة migrations دول على نسخة من قاعدة البيانات فيها كل الجداول والصلاحيات بتاعتك (شامل كل تصحيحات الأمان اللي عملناها)، وطبّقتهم بنجاح من غير أي خطأ، وبعدين شغّلت **47 اختبار جديد** عليهم تحديدًا (تفاصيلهم تحت). كل الـ88 اختبار أمني القديم و35 اختبار دورة الطلب الكاملة لسه شغّالين زي ما هم من غير أي تغيير.

---

## 3. الفحص اليدوي — بالترتيب ده بالظبط

افتح الموقع بحساب Super Admin وحساب عميل تاني (اعمل حساب عميل تجريبي جديد لو معندكش، وحساب مندوب لو محتاج).

### أ) وضع "الأقسام مقفولة" (الميزة الرئيسية الجديدة)

1. من **لوحة التحكم ← الإعدادات**: هتلاقي قسمين جداد: **"الرسمة التوضيحية (جوه الهيرو)"** و**"عرض الأقسام للعميل"**.
2. غيّر **عرض الأقسام** لـ **"متوقف"** واحفظ.
3. افتح الصفحة الرئيسية بحساب عميل: المفروض الأقسام تختفي، ويظهر بدالها كارت "مش لاقي اللي محتاجه؟" يوديك لصفحة السلة.
4. ارفع صورة لكارت "ابدأ طلبك" من الإعدادات، وتأكد إنها ظهرت في الكارت.
5. رجّع **عرض الأقسام** لـ **"مفعّل"** — تأكد إن الأقسام رجعت زي ما كانت وإن باقي الصفحة (البانر، إلخ) ما اتأثرش.

### ب) إضافة صنف يدوي للسلة — الحالتين

من صفحة **السلة** اضغط **"إضافة صنف للطلب"**:

6. اختار **"كمية محددة"**: اكتب اسم صنف، كمية، وحدة، وارفع صورة اختيارية. أضِفه للسلة وتأكد إنه ظهر بالكمية والوحدة الصح.
7. افتح المودال تاني واختار **"بميزانية تقريبية"**: اكتب اسم صنف ومبلغ (مثلاً 150). أضِفه وتأكد إنه ظهر في السلة بنص "بميزانية تقريبية 150 ج.م" من غير حقل كمية.
8. أكمل للـ Checkout وابعت الطلب. تأكد إنه اتبعت من غير خطأ.

### ج) شاشة "طلبات منتجات جديدة" (صفحة أدمن جديدة بالكامل)

9. بحساب أدمن، من القائمة الجانبية افتح **"طلبات منتجات جديدة"**. المفروض تلاقي الصنف اللي أضفته في الخطوة 7 (بميزانية) ظاهر، ومعاه اسم العميل ورقمه ورقم الطلب.
10. اضغط **"إضافة كمنتج"**: هيفتح نموذج المنتج الأصلي وبياناته معبّاة تلقائيًا (الاسم، الوصف من التعليق، الصورة لو رفعتها، التصنيف المقترح). عدّل السعر واحفظ.
11. تأكد إن المنتج ظهر في **صفحة المنتجات** العادية، وإن الطلب اختفى من قائمة "طلبات منتجات جديدة".
12. جرّب طلب تاني واضغط **"تجاهل"** بدل الإضافة. تأكد إنه اختفى من القائمة، وإن الطلب الأصلي (لو فتحته من صفحة الطلب نفسها) لسه فيه الصنف زي ما هو.

### د) حذف منتج (ميزة جديدة، لازم تتفحص بحرص)

13. من صفحة المنتجات، افتح منتج **لسه محدش طلبه** واضغط **"حذف المنتج نهائيًا"**. المفروض يتمسح فعلاً ويختفي من القائمة.
14. افتح منتج **سبق طلبه في طلب قديم** واضغط حذف. **المفروض تظهر رسالة واضحة إنه مينفعش يتمسح** (مش خطأ تقني غامض)، والمنتج يفضل موجود. لو اتمسح فعلاً أو ظهر خطأ مش مفهوم، ده خطأ ولازم تبعتلي تفاصيله.

### هـ) دورة الطلب بصنف بالميزانية (المندوب)

15. بحساب مندوب، اقبل الطلب اللي فيه الصنف بالميزانية (من الخطوة 8)، وادخل شاشة "تسجيل الشراء". تأكد إن الصنف ظاهر بنص "بميزانية تقريبية" والمكان المخصص للسعر مكتوب فيه "المبلغ اللي دفعته فعليًا" بدل "السعر الفعلي".
16. سجّل السعر واعتمد الفاتورة. تأكد إن الإجمالي في الفاتورة صح ومفيهوش قيم فاضية أو أخطاء حسابية.

---

## اللي اتفحص فعليًا (وتقدر تثق فيه من غير ما تعيده)

جربت الأربعة migrations على نسخة كاملة من قاعدة بياناتك (بكل الجداول والصلاحيات والتصحيحات الأمنية)، وشغّلت عليها اختبارات فعلية بأدوار مختلفة (عميل، عميل مهاجم، مندوب، أدمن، زائر غير مسجّل). أهم اللي اتأكد منه:

- الصنف بالميزانية بيتخزن صح (من غير كمية أو وحدة)، ومرفوض لو القيمة صفر أو سالبة أو فوق 100,000 جنيه.
- التصنيف المقترح ورابط الصورة لو مزيّفين (مش من Cloudinary، أو تصنيف غير موجود) بيترفضوا بهدوء من غير ما يوقفوا الطلب.
- **العميل والمندوب مش عندهم صلاحية إنهم يحوّلوا طلب لمنتج أو يتجاهلوه** — أدمن بس، واتأكدت إن زائر غير مسجّل برضو مرفوض.
- طلب منتج بالميزانية من عميل معين **مش ظاهر لعميل تاني** خالص.
- الفاتورة بتحسب الصنف بالميزانية صح (بسعره الفعلي، مش مضروب في كمية فاضية زي ما كان ممكن يحصل قبل الإصلاح في نفس التحديث).
- حذف منتج: العميل والمندوب مرفوضين تمامًا، الأدمن يقدر يحذف منتج مش متبوع، ومرفوض يحذف منتج له تاريخ طلبات (مايتكسرش تاريخ الطلبات القديمة).
- إعداد "عرض الأقسام" مقروء لأي زائر لكن **Super Admin بس** يقدر يغيّره.

**اللي محتاج عينك أنت بس:** الشكل والتنسيق على الموبايل، وتجربة الاستخدام الفعلية (هل الخطوات مفهومة؟)، والتأكد إن رفع الصور شغّال فعليًا مع حساب Cloudinary بتاعك.
